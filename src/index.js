let nome = "Matheus";
let nivel = 1000;

if (nivel <= 1000){
    console.log("O Herói de nome " + nome + " está no nível de " + nivel);
} else if (nivel >= 1001 && nivel <= 2000){
    console.log("O Herói de nome " + nome + " está no nível de " + nivel);
}